# hms_project
HOW TO RUN – Hospital Management System

1. Install **Node.js**
2. Install **MongoDB** (run MongoDB service locally)
3. Extract the project (unzip)
4. Open **terminal** inside project folder
5. Run:
   npm install
6. Then run:
   npm run dev
7. Open browser:

   ```
   http://localhost:3000
   ```
